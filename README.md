# The-C-Programming-Language-Kernighan-and-Ritchie-Exercises

How to compile and run:
`cc <program name.c> -o <program name>`

example:
`cc main.c -o main`

To run:
`<./program name>`

example:
`./main`
